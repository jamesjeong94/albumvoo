import React from 'react';

interface StreamListProps {
  items: any[];
}

const StreamList: React.FC<StreamListProps> = ({}) => {
  return (
    <div>
      <p>Stream</p>
    </div>
  );
};

export = StreamList;
